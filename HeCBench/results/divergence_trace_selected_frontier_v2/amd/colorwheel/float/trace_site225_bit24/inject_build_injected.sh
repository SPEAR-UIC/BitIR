FI_SITE="${SITE_ID}" FI_BIT="${BIT_INDEX}" FI_TARGET="${INJECT_TARGET}" FI_INT_FLOAT_ONLY="${INT_FLOAT_ONLY}" FI_INCLUDE_CONSTANTS="${INCLUDE_CONSTANTS}" "${BITIR_MACHINE_CLANGXX}" -x hip --offload-arch="${BITIR_MACHINE_HIP_ARCH}" --hip-path="${BITIR_MACHINE_RUNTIME_HOME}" -O0 -g -I "${BENCH_DIR}" -I "${REPO_ROOT}/HeCBench/src" -fpass-plugin="${PLUGIN}" "${SRC}" -o "${BIN_PATH}"
