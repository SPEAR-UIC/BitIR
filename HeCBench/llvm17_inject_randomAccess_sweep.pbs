#!/bin/bash
#PBS -A UIC-HPC
#PBS -N andomAccess
#PBS -q prod
#PBS -l select=25:ncpus=1:ngpus=4:mem=16gb
#PBS -l filesystems=home:eagle
#PBS -l walltime=06:00:00
#PBS -o OUT_randomAccess_llvm17_inject.out
#PBS -e ERROR_randomAccess_llvm17_inject.err

set -euo pipefail

export BENCH="${BENCH:-randomAccess}"
export COMPARE_MODE="${COMPARE_MODE:-exact}"

bash "${PBS_O_WORKDIR}/HeCBench/llvm17_inject_bench_sweep.pbs"
