#!/bin/bash
#PBS -A UIC-HPC
#PBS -N bsearch
#PBS -q prod
#PBS -l select=50:ncpus=1:ngpus=4:mem=16gb
#PBS -l filesystems=home:eagle
#PBS -l walltime=06:00:00
#PBS -o OUT_bsearch_llvm17_inject.out
#PBS -e ERROR_bsearch_llvm17_inject.err

set -euo pipefail

export BENCH="${BENCH:-bsearch}"
export COMPARE_MODE="${COMPARE_MODE:-exact}"

bash "${PBS_O_WORKDIR}/HeCBench/llvm17_inject_bench_sweep.pbs"
