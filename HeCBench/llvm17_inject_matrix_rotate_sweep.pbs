#!/bin/bash
#PBS -A UIC-HPC
#PBS -N matrix_rotate
#PBS -q prod
#PBS -l select=10:ncpus=1:ngpus=4:mem=32gb
#PBS -l filesystems=home:eagle
#PBS -l walltime=03:00:00
#PBS -o OUT_matrix_rotate_llvm17_inject.out
#PBS -e ERROR_matrix_rotate_llvm17_inject.err

set -euo pipefail

export BENCH="${BENCH:-matrix-rotate}"
export COMPARE_MODE="${COMPARE_MODE:-float}"

bash "${PBS_O_WORKDIR}/HeCBench/llvm17_inject_bench_sweep.pbs"
