#!/bin/bash
#PBS -A UIC-HPC
#PBS -N atomicCost
#PBS -q prod
#PBS -l select=30:ncpus=1:ngpus=4:mem=32gb
#PBS -l filesystems=home:eagle
#PBS -l walltime=06:00:00
#PBS -o OUT_atomicCost_llvm17_inject.out
#PBS -e ERROR_atomicCost_llvm17_inject.err

set -euo pipefail

export BENCH="${BENCH:-atomicCost}"
export COMPARE_MODE="${COMPARE_MODE:-exact}"

bash "${PBS_O_WORKDIR}/HeCBench/llvm17_inject_bench_sweep.pbs"
